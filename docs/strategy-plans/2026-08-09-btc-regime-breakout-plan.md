# BTC 多周期状态门控突破策略规划（2026-08-09）

## 决策

本轮不创建、不审计、不绑定新的策略库版本。现有引擎可表达的候选在费用后均未通过预先冻结的收益门槛；把其中“最不亏”的参数保存为新策略会制造错误的盈利暗示。当前本地 Live 和公网 Paper 的绑定均保持不变。

下一代候选命名为 `BTC Regime Breakout v2`。它不是现有 SMA/RSI 参数的又一次搜索，而是针对本轮确认的主要失效原因设计：持续状态条件会反复入场，1 分钟噪声和 10 bps 手续费加 10 bps 滑点会吃掉接近零的毛优势。

## 本轮证据

统一费用口径为初始资金 10 USDT、手续费 10 bps、滑点 10 bps；所有分析均只读，没有创建研究记录、切换绑定或触发生产评估。

| 检验 | 数据 | 候选 | 结果 |
|---|---:|---:|---|
| 原生 1m 多因子 | 最新连续 500 根，0 缺口 | 355 | 验证正收益 0 |
| 1m 顺势回调 | 最近 7 天 10,080 根，0 缺口；前 5 天开发 | 18 | 合格 0；最好窗口中位数 -0.2703%，正窗口 0/24 |
| 1m 真实 SMA 交叉原型 | 最近 7 天 10,080 根，0 缺口；前 5 天开发 | 9 | 合格 0；最好窗口中位数 -0.4077%，正窗口 2/24 |
| 5m 趋势动量放量 | 最近 28 天 8,064 根，0 缺口；前 21 天开发 | 9 | 合格 0；最好窗口中位数 -2.0813%，正窗口 0/21 |

上述跨窗口复合值只是分段压力诊断，不是连续账户收益预测；每段结束会按现有回测规则平仓。开发门槛未通过时均未读取预留 holdout。

## 新策略定义

### 市场状态门控

- 原始数据仍为 BTC/USDT 1m，由同一研究模块聚合 5m 和 60m，避免引入第二套行情或执行账本。
- 只在 60m 收盘价高于 SMA200，且 SMA200 斜率为正时允许做多。
- 状态门关闭时不新开仓；持仓按退出规则处理，不因为切换状态立即追价成交。

### 入场

- 5m 收盘价真实突破此前 20 根完整 5m K 线最高价。
- 当前 5m 成交量至少为 VOL-SMA20 的 1.5 倍。
- 当前无仓位、无待处理订单、退出冷却期已经结束。
- 同一突破事件只允许一次入场，持续位于突破位上方不能重复触发。

### 退出

- 初始保护止损：入场价下方 1 ATR(14)。
- 盈利后使用 2 ATR(14) 跟踪止损；不设置固定 2% 强制止盈。
- 60m 状态门关闭或持仓超过 48 根 5m K 线仍未产生正向进展时退出。
- 退出后冷却 12 根完整 5m K 线，必须等待新的突破事件。

### 仓位与账户风险

- 单标的最大仓位 60%。新策略不设置固定报价上限：初始账户只有 10 USDT 时，买入仍会被可用现金自然限制在 10 以内；盈利后额度可随权益增长，但始终受 60% 仓位、每笔风险和交易所约束。
- 每笔计划风险不超过账户权益 0.5%；交易所最小名义额无法满足时直接 HOLD，不放大风险迁就下单。
- 策略最大回撤门槛 3%，每日亏损门槛 2%，每小时最多一组买卖。
- 退出允许卖出完整管理仓位，不设置额外卖出名义金额上限。

## 所需的最小引擎能力

1. 在现有条件与回测引擎中增加多周期聚合、真实 `breakout_above` 事件、ATR 和冷却状态；不建立第二套策略执行算法。
2. 研究、Paper 和 Live 共用相同条件语义、指标预热和下一根完整 K 线成交契约。
3. 回测加入 Binance 最小名义额、数量步进和尘埃处理，避免 10 USDT 小账户出现不可成交的纸面优势。
4. 保持研究端点与执行端点隔离；创建 run、experiment 或 library item 永远不能自动绑定或评估。

## 正式验证门槛

- 使用至少 90 天连续 1m 原始数据，先冻结参数空间，再按 60/20/20 时间顺序切分。
- 费用和滑点各 10 bps，并执行 Binance 数量精度与最小名义额约束。
- validation 与唯一一次 test 均须收益率大于 0、Profit Factor 至少 1.2、最大回撤不超过 3%。
- 至少 30 次完整往返，滚动窗口正收益比例至少 60%。
- 胜出参数的相邻窗口组合也必须为正，拒绝孤立尖峰参数。
- 任一门槛失败时保持研究草案，不进入策略库 `audited` 状态。

## 发布顺序

1. 独立分支实现条件语义和测试，不重启当前 Docker Live。
2. 用新鲜、未查看的数据创建一次 P0 源 run 和一次 experiment。
3. winner 通过唯一 holdout 后，用新快照重新 P0 审计并只保存到策略库。
4. 先完成至少连续 `7 × 24` 小时的 Paper forward test；当前实现不会在第 7 天自动停止，启动前必须另建到期暂停与收益汇总机制，或明确人工停止责任。期间不得交接到 Local Live。
5. 只有当前真实仓位已退出、监控暂停、无 pending/unresolved、重新显式授权后，才讨论生产绑定。

## 当前隔离边界

截至 2026-08-10 本轮运行前回读，本地自动交易处于 Paper 模式并暂停，账户初始资金、现金和权益均为 10，仓位为 0、无 pending，旧 binding 未变；Live、订单提交和路由边界均关闭。本轮没有向公网发出 formal、promotion、binding 或启动写请求，因此本文不声称公网周测已启动，也不把“未写入”误作公网当前状态证明。

## 90 天开发门复核（2026-08-09）

本轮随后读取 Binance `BTC/USDT · 1m` 连续窗口 `2026-05-02T02:00:00Z` 至 `2026-08-09T07:00:00Z`，共 `142,860` 根、`0` 缺口；其中前 `221` 小时只作为 SMA220 最大候选的预热，正式可评分窗口仍为 90 天。最后 18 天 test 未读取。

预注册基准网格 `SMA {180,200,220} × breakout {18,20,22}` 的 9 个候选全部未通过开发/验证门。验证收益区间为 `-9.6151%` 至 `-11.3831%`，Profit Factor 为 `0.0324` 至 `0.0452`，最大回撤为 `9.6151%` 至 `11.3831%`；“相对最好”仍是明确亏损，不具备进入 holdout 的资格。

为判断失败是否主要来自 ATR 过紧，只在已经暴露的前 72 天开发区额外冻结并运行 `breakout {20,40,60} × initial ATR {2,3} × trailing ATR {3,4}` 的 12 个邻域诊断，成交量倍数固定 `1.25`。验证收益仍全部为负，区间 `-3.0088%` 至 `-3.3180%`，Profit Factor 为 `0.0894` 至 `0.1048`，验证完整往返 `12` 至 `15`，同时未达到正收益、PF `1.2`、回撤 `3%` 和至少 30 次完整往返门槛。

因此本轮终止于开发门：不消费唯一 test、不创建 audited 策略、不绑定 Paper/Live，也不启动任何新策略。以上结果只证明这两个冻结参数族未通过，不能推导未来不存在盈利策略，也不能构成收益保证。

## 其它低换手策略族诊断（2026-08-09）

为避免把结论限定在突破策略，本轮又在物理截止于 `2026-07-22T07:00:00Z` 的开发数据上预注册 3 个本质不同的低换手策略族，共 75 个候选。输入为 Binance `BTCUSDT 1m` 半开区间 `[2026-05-02T02:00:00Z, 2026-07-22T07:00:00Z)` 的 `116,940` 根 K 线，0 缺口、0 重复；诊断数据 SHA-256 为 `f0a48ec55b8bf83f1f0b1ffe3a802fbe4ead9923c8b18cfe50199345067fa897`。最后 18 天 holdout 仍未读取。

统一使用初始资金 10 USDT、仓位上限 60%、买入不超过 10 USDT、完整退出不限 10 USDT、买卖双边各 10 bps 手续费和 10 bps 滑点、信号后下一分钟开盘成交。7 个互不重叠的滚动 validation 窗要求至少 5 个正收益。

| 策略族 | 相对最好参数 | 固定 validation 收益 / PF / DD | 完整往返（val / dev） | 滚动正窗 | 结论 |
|---|---|---|---:|---:|---|
| 15m 双均线趋势 | fast 21 / slow 48 | `+1.9000% / 1.4872 / 2.8405%` | `12 / 48` | `3 / 7` | 慢线参数位于边界，滚动门失败 |
| 15m 波动带恢复 | N 96 / Z 2.5 | `+1.6125% / 3.6714 / 1.0464%` | `8 / 37` | `4 / 7` | Z 位于边界，滚动与四邻门失败 |
| 4h 低波动动量 | lookback 12 / cap 0.01 | `+0.4349% / 1.3812 / 3.0652%` | `4 / 20` | `2 / 7` | 回撤、往返、滚动与邻域均失败 |

三个策略族最终合格候选均为 0。此次为进程内只读诊断，`holdoutRowsRead=0`、无 POST、无落库、无绑定、无运行态修改；它没有形成 sealed dataset、逐候选不可变审计或正式 promotion lineage，因此只能作为下一轮设计依据，不能作为策略库晋级证据。

## Regime Breakout v2.1 重试预注册（2026-08-10）

在读取本轮 validation 或 test 结果前，冻结一次短状态窗口重试，不再扩展参数空间：

- 评分区间固定为 `[2026-05-12T04:00:00Z, 2026-08-10T04:00:00Z)`，恰好 90 天；额外使用 `[2026-05-08T19:00:00Z, 2026-05-12T04:00:00Z)` 的 4,860 根 1m K 线作为最大候选预热，不计入收益。
- development 截止 `2026-07-23T04:00:00Z`；最后 18 天为一次性封存 test，正式候选通过开发门前不得读取。
- 基准固定为 5m 突破 8 根、VOL-SMA20 倍数 1.0、ATR14 初始 1 倍/盈利后跟踪 2 倍、最长持仓 48 根、退出冷却 12 根。
- 唯一参数维度为 60m 状态 SMA 窗口 `{20, 40, 80}`；只有中间候选具备完整左右邻域，边界候选不能通过稳定性门。
- 仓位、风险和成本保持不变：最大仓位 60%、每笔风险 0.5%、不设置固定报价上限、完整退出不设名义额上限，手续费和滑点各 10 bps/side。初始 10 USDT 账户仍由可用现金限制，盈利后才随权益动态增长。
- 服务端固定门槛为 development 至少 30 次完整往返；validation/test 各自收益大于 0、PF 至少 1.2、回撤不超过 3%、至少 6 次完整往返；7 个互不重叠滚动窗至少 5 个为正；左右相邻候选 validation 都必须为正。
- 若未通过，停止本轮，不消费或不重复读取 test、不改选第二名、不绑定、不启动；若全部通过，才用不同的新鲜封存快照重新 P0、写入 promotion lineage、绑定并仅启动 Paper。

选择这个区域的理由不是已知盈利，而是此前长周期 SMA180–220 与 breakout18–60 已明确失败；本轮用更短状态窗口和更低放量门验证“样本不足/响应过慢”这一预先提出的假设。结果仍须由上述冻结门槛决定。

### 重试结果

本地 sealed P0 `run-4fd5256032e8` 成功封存 Binance `BTC/USDT · 1m` 数据集 `sealed-0e2c733a4984999b7122e8dc`：总计 134,460 根，development 108,540 根，withheld test 25,920 根，0 缺口；策略修订为 `d9fde2db89c4`，固定额度为 `null`，即盈利后按权益、仓位、风险预算和可用现金动态 sizing。

正式实验 `experiment-30e84b74f50145edbc0055be404ae83e` 按预注册定义完成 48 次 development/validation/滚动评估，结果哈希为 `d86b56b2cb202b9744d20287f954bc6e0813b97409ef4577725c75bc8a02f0b3`。三个候选均未进入 test：

| 60m SMA | validation 收益 / PF / DD | validation 往返 | development 往返 | 滚动正窗 | 结论 |
|---:|---|---:|---:|---:|---|
| 20 | `-2.8538% / 0.0036 / 3.1695%` | 11 | 24 | 0 / 7 | 收益、PF、DD、样本、滚动均失败 |
| 40 | `-3.0711% / 0.0034 / 3.1625%` | 12 | 25 | 0 / 7 | 中心及两个邻域均为负，全部门槛失败 |
| 80 | `-3.2793% / 0 / 3.2793%` | 13 | 27 | 0 / 7 | 收益、PF、DD、样本、滚动均失败 |

实验终态为 `no_validation_candidate`，`profitabilityGatePassed=false`、`selectedCandidateId=null`、`holdoutStatus=unconsumed`。因此没有 fresh P0 promotion、没有新的 `audited` 可绑定版本、没有切换 binding，也没有启动 Paper 或 Live。继续把其中“最不亏”者包装成盈利策略会违反本轮冻结门槛。

## 工程实现状态（本地候选流程，2026-08-10）

当前工作树已实现本地 v2 formal 流程的工程能力：结构化策略配置、研究/Backtest/Paper 共用评估器、按不超过 500 根分页物化的不可变 sealed dataset、development 与唯一 test 分区隔离、异步正式实验、唯一 rank-1 holdout claim、fresh sealed P0 promotion lineage，以及 Paper-only 绑定复核。门槛失败的候选不能进入 `audited`，缺失或漂移的证据均失败关闭。

以上只是代码与测试能力，不是盈利保证。本次真实重试同样没有候选通过正式收益门，因此没有新的 `audited` 可绑定版本、promotion 或新策略启动。测试中的盈利夹具不能替代真实市场证据。

公网 `myqt.zxai.fun` 尚不具备 formal 端到端能力：public P0 对 sealed dataset 明确返回 `sealed_dataset_local_only`，前端目前也不能创建或解析 formal v2 实验及执行 promotion。不得据此声称公网 formal 可用或公网周测已启动。

即使候选通过，fresh P0 promotion、显式 Paper 绑定和显式启动仍是相互独立的操作。绑定动作必须保持 `executionMode=paper`、`status=paused`、`enabled=false`，不会触发 Stage 10、立即评估或订单提交；v2 当前不允许 Local Live。

当前系统没有 7 天 Paper 自动停止或自动收益汇总机制。启动周测前必须另行建立到期暂停与汇总自动化，或记录明确的人工停止流程。

动态额度、fresh P0 盈利复核与 sealed 数据完整性改动已通过 `1158 passed, 2 skipped, 869 subtests` 的全量后端回归，Web 生产构建通过，API 镜像已重建并健康运行。重建后回读仍为 `paper + paused + flat + no pending`，现金和权益均为 10，旧 binding 未变，Live/订单提交/路由仍全部阻断。由于真实正式实验未通过，本轮工程能力已部署到本地，但新策略本身没有发布、绑定或启动。
