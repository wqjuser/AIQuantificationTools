# Stage 6 委托必须来自权威证据链

Stage 6 只接受从同一条 Stage 4 canonical workflow、Stage 5 reconciled shadow session、readiness、preflight 和 approved authorization review 派生的 Sandbox 委托候选。UI 和 API 不提供绕过该链路的手填提交入口。这样可以复用现有风险、审批、稳定 `clientOrderId` 和审计验证，并避免出现第二套订单与授权模型。
