# 行情中心与运行管理 UI 验收

本轮修复针对行情中心的两个视觉与交互问题：自选概览被等高布局拉成巨型空卡，以及“高级功能与证据”使用原生 `details` 伪装成全屏浮层。

后续复验又发现顶栏与页面各保留了一个行情输入框、缓存状态贴住表格边界、K 线信息条缺少留白，以及高级工作区重复渲染整块主图和行情摘要。本目录同时保留这轮结构收敛后的验收结果。

## 调整结果

- 自选概览改为紧凑的涨跌宽度摘要，并复用现有 watchlist 数据展示市场分布。
- 单个概览统计项高度由 `218.5px` 收敛为 `58px`，不再用空白撑高数字卡片。
- 删除顶栏“高级功能与证据”入口及 `dialog` 浮层，在左侧“治理与系统”分组新增“运行管理”菜单。
- 运行管理使用现有工作区路由，可通过 `workspace=operations` 直接打开和刷新恢复。
- 高级内容继续复用既有模型、API、store 和证据面板，没有引入第二套工作区实现。
- 删除页面内没有行为的第二个搜索框，只保留顶栏真实标的切换输入。
- 重新分配自选表末列宽度并居中缓存状态，桌面端左右留白均为约 `8px`。
- K 线底部信息条增加 `4px 10px 5px` 内边距，避免与图表和容器边界粘连。
- 运行管理不重复主图、行情卡与指标卡，只保留研究运营队列、数据源证据、扫描器和流程节点；桌面端使用双栏，移动端使用单列卡片。

## 截图

- `01-before-expanded.png`：修改前的浮层。
- `02-after-compact-overview.png`：修改后的行情中心与自选概览。
- `03-after-dialog.png`：独立页面改造前的高级证据对话框。
- `04-mobile-dialog.png`：独立页面改造前的 375px 对话框。
- `05-after-market-controls.png`：去除重复输入并修正表格、K 线间距后的行情中心。
- `06-after-evidence-workspace.png`：独立页面改造前、已移除重复行情内容的工作区。
- `07-operations-page.png`：左侧菜单进入的独立运行管理页面。

## 验收环境

- 视口：`1676 × 962`、`1280 × 720`、`375 × 812`
- 地址：`http://127.0.0.1:5173/?workspace=operations&market=ashare&symbol=600000&timeframe=1d`
- Docker Web 与 API 容器均为 `healthy`。
