# Stage 9 退出不要求生产账户入金

Stage 9 的退出验收不要求向生产账户充值，也不要求生产账户通过候选资金检查。默认 CI 使用确定性只读替身覆盖完整成功与故障路径，Docker 在无生产凭据时证明 fail closed；显式真实只读验收允许账户因资金不足得到安全的 `blocked`，但必须证明证据脱敏、重启回读一致且没有订单、成交、转账或提现调用。真实资金和生产交易凭据只属于后续独立试单授权阶段。
