# AIQuant Terminal

本上下文描述本地优先量化研究、模拟执行与受控交易路由中的核心产品概念。

## Language

**Stage 0 CI Artifact Node 24 收口**：
GitHub Actions 的六类验收清单统一由原生 Node 24 的 `actions/upload-artifact@v7` 上传，不再依赖强制 JavaScript action 运行时变量。artifact 名称、路径、`if: always()` 和产品验收顺序保持不变。
_Avoid_: action wrapper、额外 job、自托管 runner 兼容层

**Stage 0 SQLite 测试资源生命周期收口**：
本批修复的 23 处 Python 测试连接同时保留原有事务上下文和显式关闭：先由连接上下文提交或回滚，再由 `contextlib.closing` 关闭连接。契约测试只负责拒绝新增裸 `with sqlite3.connect(...)`，避免把事务上下文误当成资源关闭并在 Python 3.14 下留下 `ResourceWarning`；既有只读或显式提交的 `closing` 用法、生产 store 的关闭行为均不变。
_Avoid_: 第二套数据库 helper、生产 store 重构、全局 warning 框架

**Stage 0 发布门禁稳定性收口**：
PR 和 `main` 各运行一次完整质量门禁，feature branch 不再因 `push + pull_request` 重复执行；Docker Web 的 `/api/` upstream read timeout 与现有 90 秒 smoke 请求预算一致，避免 P1 长刷新先被 Nginx 以 504 切断。它不改变 P1 业务语义或任何交易能力。
_Avoid_: 通用重试框架、Stage 9、生产订单授权

**Stage 0 P1 验收工作量收口**：
P1 Docker acceptance 仍完整校验工作区至少有 3 个标的，但只按工作区顺序刷新前三个固定验收样本，避免产品自选增长无界放大发布门禁的外部请求耗时。产品端完整 watchlist refresh、行情 Provider 和 90 秒请求预算不变。
_Avoid_: 通用重试、并发刷新框架、产品自选上限

**Stage 0 Stage 6 启动等待收口**：
Stage 6 acceptance 在初次 `docker compose up -d` 后复用已有 `_wait_for_api`，允许 Web/API 在现有 30 秒预算内完成健康恢复，避免单次探测撞上容器启动时的连接重置。重启恢复继续复用同一函数。
_Avoid_: 固定 sleep、第二套重试器、放宽 Sandbox 安全边界

**Stage 8 产品阶段状态收口**：
前端唯一产品阶段模型对 Stage 0 至 Stage 8 已退出事实的同步：全部阶段为 maintenance、Execution 归属 Stage 8、当前阶段集合为空。它只影响导航中的交付状态说明，不改变 Stage 6/7/8 执行能力或审计证据。
_Avoid_: Stage 9、动态阶段配置、生产订单授权

**Stage 9 生产委托准入准备**：
从权威 Sandbox 执行与当前生产只读证据生成一次性生产委托准入候选，并记录不可改写人工复核的阶段。它不接触生产交易凭据，也不创建、查询、撤销或同步生产委托。
_Avoid_: 生产执行、真实资金试单、实盘订单管理

**生产委托准入准备**：
在开放真实资金委托前，对凭据隔离、风险上限、人工授权、急停和恢复边界形成可审计结论的阶段。该结论不允许创建、查询、撤销或同步生产委托。
_Avoid_: 实盘执行、生产试单、生产委托授权

**生产交易凭据**：
允许真实资金现货交易的专用交易所凭据。生产委托准入准备不读取、保存或探测此类凭据，只允许继续使用生产只读凭据。
_Avoid_: 生产只读凭据、Sandbox 凭据、准入准备凭据

**生产委托准入账户**：
生产委托准入准备唯一面向、且由本系统自动交易策略独占使用的整个 Binance Spot 现货账户。账户中的全部非零现货资产都属于策略账户范围；它不包含 Futures、Margin、多个账户或其它交易所。
_Avoid_: 与人工交易共享的现货账户、通用交易账户、多账户池、多交易所账户

**生产委托准入候选**：
绑定单个权威工作流、批次和全部精确订单内容哈希的一次性准入结论。它不是账户级许可，不能打开全局实盘能力，也不能直接提交委托。
_Avoid_: 实盘许可证、账户授权、全局交易开关

**生产委托准入证据链**：
由同一权威工作流的 Stage 6 已成功对账终态批次和当前 Stage 8 生产只读连续性共同组成的候选来源。缺少任一环节、仅有全局退出清单或手填订单都不能形成生产委托准入候选。
_Avoid_: 手工订单、全局退出证明、跨工作流证据拼接

**生产市场规则复核**：
使用当前 Binance Spot 公开市场规则重新校验候选订单的精度、最小数量和最小金额。规则漂移只会阻断候选，不会自动缩量、改价、拆单或改写原始订单。
_Avoid_: 自动修正、隐式重新规范化、生产订单改写

**生产候选价格闸门**：
使用不超过 30 秒的 Binance Spot 公开参考价检查候选限价；买价高于或卖价低于参考价超过 1% 时阻断。行情缺失、过期或不可验证时同样阻断。
_Avoid_: 跨交易所报价、过期行情、价格自动修正

**生产候选资金检查**：
使用生产只读凭据判断准入候选逐单所需资金或资产是否充足。审计证据只保留通过或阻断结论及观测时间，不保存资产名称、余额或交易所原始响应。
_Avoid_: 生产余额快照、资产明细、交易凭据检查

**首版生产准入包络**：
生产委托准入候选只允许 `BTC/USDT`、`ETH/USDT` 的 GTC 限价单，最多两单、每单不超过 10 USDT、批次不超过 20 USDT。任一订单超出包络时整个候选被阻断。
_Avoid_: 市价单、其它交易对、自动拆单、部分准入

**生产委托准入复核**：
对单个生产委托准入候选记录的一次不可改写人工结论，包含具名操作者、原因和固定确认项。复核通过不产生生产执行权，也不需要用户体系、RBAC 或双人审批。
_Avoid_: 生产执行授权、可改写审批、角色权限系统

**生产委托准入急停**：
Stage 8 生产只读访问控制在准入准备中的复用：`revoke` 会在生产网络访问前阻断候选生成和复核，`restore` 继续要求当前生产路由复核。本阶段没有生产订单，因此不创建第二套生产 kill switch。
_Avoid_: 独立准入开关、生产订单撤销器、重复急停状态

**生产委托准入新鲜度**：
生产委托准入候选生成后 10 分钟内可以复核；复核时必须重新验证连续性、市场规则、30 秒行情和资金充足性。过期或任一证据漂移后只能生成新候选，不能续期或修改原候选。
_Avoid_: 候选续期、原地更新、沿用过期检查

**Detached 生产准入审计副本**：
从研究包导入的生产委托准入候选和复核，只能校验与回放，不能继续复核、连接生产环境或恢复准入权。权威准入状态只来自当前 API 数据卷中的本地审计账本。
_Avoid_: 可执行导入、跨环境准入恢复、导入后继续复核

**生产只读连续性**：
Stage 8 从最新 Stage 7 probe、Stage 6 exit、production route review 和本地访问控制派生的当前运维状态，区分 `current`、`stale`、`blocked`、`revoked` 与 `missing`。它只引用脱敏证据，不重新访问交易所。
_Avoid_: 后台账户同步、生产订单监控、自动探针

**生产只读访问控制**：
Stage 8 保存在当前 API 数据卷中的人工 `revoke / restore` 事件。revoke 在生产网络访问前阻断 Stage 7；restore 必须绑定当前 production route review。它不自动撤销交易所 Key，也不赋予生产委托权。
_Avoid_: 生产 kill switch、交易所 Key 管理、实盘授权

**Stage 8 真实恢复退出验收**：
在已有 current Stage 7 生产只读证据、当前 route review 和专用只读凭据的数据卷中，复用 Stage 8 revoke/restore 与 Stage 7 probe 完成一次真实恢复演练。验收必须证明 revoke 在网络前阻断、restore 链接本次 revoke、新 probe 权限安全、continuity 恢复 current 且 API 重启后 probe/control hash 一致；它仍不授权生产订单或资金操作。
_Avoid_: Key 自动轮换、生产订单恢复、live route 验收

**生产只读准入**：
Stage 7 对 Binance Spot 生产环境执行的只读连接验收，只允许检查市场元数据、API Key 权限和脱敏账户摘要。它不创建、查询或撤销生产委托，也不构成实盘授权。
_Avoid_: 实盘接入、生产委托路由、生产交易

**生产只读凭据**：
API 服务中仅用于 Stage 7 的独立 Binance Spot 生产环境凭据，其交易、提现和内部转账权限必须关闭。它不能复用 Sandbox 或通用 CCXT 凭据。
_Avoid_: 实盘交易凭据、Sandbox 凭据、通用 CCXT Key

**脱敏生产账户摘要**：
Stage 7 从生产账户读取后形成的最小审计投影，只包含账户类型、权限布尔值、非零资产数量和时间信息，不包含资产名称、余额、订单、成交或原始交易所响应。
_Avoid_: 账户持仓、资产明细、余额快照

**Stage 7 退出验收**：
由默认无凭据 fail-closed 清单和一次真实 Binance Spot 生产只读清单共同组成。真实清单必须证明全部 mutation 权限关闭、账户摘要脱敏、API 重启回读 hash 一致，并继续固定所有 live/order 边界为关闭。2026-07-13 已完成该验收并转入 maintenance；该结论不授权生产订单或真实资金路由。
_Avoid_: 实盘授权、生产下单验收、账户资产快照

**Sandbox 委托路由**：
只连接交易所测试网、只使用无真实价值测试资产的委托通道。它可以提交、查询、撤销和对账测试网委托，但不构成实盘授权。
_Avoid_: 实盘路由、模拟成交、Paper Trading

**Sandbox 交易场所**：
Stage 6 唯一允许连接的交易场所，固定为通过 CCXT 接入的 Binance Spot Testnet。它不包含 Futures、杠杆、保证金或其它交易所。
_Avoid_: Binance Futures Testnet、生产环境、通用多交易所路由

**Sandbox GTC 限价委托**：
Stage 6 首版唯一允许提交的测试网委托类型，必须同时包含买卖方向、交易对、数量和限价，并固定使用 GTC。市价单、IOC、FOK 和其它订单类型不在首版范围内。
_Avoid_: 市价委托、实盘委托、模拟成交

**Sandbox 委托候选**：
从同一条 Stage 4 canonical workflow、Stage 5 reconciled shadow session、readiness、preflight 和 approved authorization review 权威证据链派生的测试网委托意图。只有候选可以进入 Stage 6 提交流程，不能通过 UI 或 API 任意手填委托绕过证据链。
_Avoid_: 手工委托、临时订单、独立 Sandbox 订单入口

**Sandbox 批次授权**：
操作者对一个 Sandbox 委托候选批次作出的一次性提交授权，精确绑定 `workflowHash`、`batchId` 和全部订单内容 hash。首次提交必须在授权后 10 分钟内开始；同一内容的幂等恢复不受过期影响，批次或订单内容变化后必须重新授权。
_Avoid_: 长期授权、全局授权、逐单授权

**Sandbox 未知提交状态**：
委托请求超时或断线后，系统无法确认交易所是否已接收委托的状态。系统必须先使用原 `clientOrderId` 查询；确认不存在时最多以同一 ID 重试一次，仍无法确认则进入 `reconciliation_required` 并停止批次。
_Avoid_: 提交失败、新 ID 补单、无限重试

**Sandbox 批次补偿**：
批次按 canonical 顺序逐单提交，任一委托被拒绝、状态未知或对账失败时立即停止后续提交，并尽力撤销已经提交但尚未成交的委托。已成交事实只能通过对账记录，不能伪造事务回滚。
_Avoid_: 原子批次、成交回滚、失败后继续提交

**Sandbox Kill Switch**：
Stage 6 唯一 Sandbox 账户的持久化急停开关。触发后阻止新批次、停止当前批次后续提交，并尽力撤销本系统创建的全部未成交委托；状态跨重启保持，完成对账后只能由人工重置。
_Avoid_: 页面临时状态、自动重置、多级急停系统

**Sandbox 规范化委托**：
使用 CCXT 市场元数据规范化数量和价格，并通过交易对、精度、最小数量/金额、Sandbox 可用余额及 Stage 4 `maxBatchNotional` 校验后的精确委托内容。批次授权绑定该内容；校验失败时不得自动缩量、改价或拆单。
_Avoid_: 原始未校验委托、提交时隐式改价、自动缩量

**Sandbox 订单状态**：
本地持久化的最小测试网订单生命周期：`authorized`、`submission_pending`、`open`、`partially_filled`、`filled`、`canceled`、`expired`、`rejected` 或 `reconciliation_required`。外部调用前必须先记录 pending 状态，交易所原始状态与成交数量作为证据保留。
_Avoid_: Shadow 状态、CCXT 原始状态副本、仅存在于页面的状态

**Sandbox 对账**：
使用同一权威函数在 API 启动恢复、提交或撤单完成、人工刷新以及 kill switch 触发时查询交易所并更新非终态订单。Stage 6 不运行常驻轮询器，所有对账结果写入审计账本。
_Avoid_: 浏览器临时刷新、后台轮询服务、另一套恢复任务

**Stage 6 退出验收**：
由无密钥确定性 CI 证据和一次真实 Binance Spot Testnet 提交、查询、撤单、对账的脱敏 manifest 共同组成。测试替身只验证故障与恢复逻辑，不是产品适配器；缺少真实 Testnet manifest 时 Stage 6 不能退出。
2026-07-13 已以 BTC/USDT、ETH/USDT 两笔终态 `canceled` 委托完成该验收，Stage 6 转入 maintenance；该结论不授权生产或真实资金路由。
_Avoid_: 仅单元测试通过、伪造交易所成功、含密钥的验收包

**Sandbox 写路由凭据**：
Stage 6 API 服务唯一允许用于测试网写操作的 `CCXT_SANDBOX_API_KEY` 与 `CCXT_SANDBOX_SECRET`。不得回退到通用或生产风格的 CCXT 变量，密钥不得进入 Web、日志、审计、导出包、镜像或 manifest。
_Avoid_: CCXT_API_KEY、CCXT_BINANCE_API_KEY、浏览器密钥配置

**Detached Sandbox 审计副本**：
从研究包导入的 Stage 6 订单与批次证据，只能用于校验和回读，不能提交、撤单、重试、对账或连接交易所。执行恢复权只来自同一 API 数据卷中的本地权威账本。
_Avoid_: 可执行导入、跨环境恢复、导入后继续路由

**Stage 6 执行黄金路径**：
Execution 工作区中的唯一测试网执行流程：检查规范化批次、一次性人工授权、提交、对账或撤单。Portfolio 只生成候选并跳转，Settings 只显示配置状态，Audit 只读回放证据。
_Avoid_: 多工作区提交入口、Settings 下单、Audit 重试

**活动 Sandbox 批次**：
唯一 Sandbox 账户中尚有 `submission_pending`、`open`、`partially_filled` 或 `reconciliation_required` 订单的批次。任一活动批次存在时不得启动新批次，完成终态对账后才释放账户。
_Avoid_: 并发批次、余额预留池、多账户调度

**实盘委托路由**：
连接真实资金账户并可能产生真实成交的委托通道。它不属于 Stage 6。
_Avoid_: Sandbox 委托路由、Shadow Execution

**受控自动生产现货会话**：
在专用生产交易凭据、Stage 10 权限核验与持久急停均有效后，由实名操作人显式开启的 `BTC/USDT` 自动交易会话。授权时长由平台设置控制，默认八小时，`0` 表示永久有效；修改只影响下一次显式授权，不能静默扩大当前活动会话。永久授权仍受人工暂停、急停、控制 ID、逐笔权限复核、余额、交易所规则和风控约束。会话只允许新增风险不超过 10 USDT 的生产现货市价单；关闭本策略持仓按原始成本校验。委托使用稳定客户端订单 ID 查询后提交，未知状态会停止后续委托。
_Avoid_: 配置保存自动授权、时长修改静默延长当前会话、其它交易对、新增风险超过 10 USDT、Futures、Margin、提现、划转、未知状态继续下单

**生产策略绑定**：
策略工坊把一个已审计的 `BTC/USDT · 1m` 策略版本及其研究运行交接给现有自动交易评估链；回测实验室只通过只读服务端预检展示该运行是否具备生产交接资格。AI 评审只把权威评审、最新人工研究决策和当前生产绑定串联展示：评审结论、决策所属评审、评审记录哈希、证据哈希和候选版本必须全部精确匹配。候选版本不同于源策略版本时，必须先采用候选草稿并从策略工坊重新生成审计证据；版本相同时，服务端预检的运行、策略版本和快照身份仍须一致，且失效或阻断的旧绑定不得显示为可用，也只能前往回测实验室完成实名交接。预检与实际交接复用同一套规范版本、快照身份、回测指标、成交与保守费用重放校验，实际交接前再次完整复核。绑定时固定策略快照、完整哈希、审计运行 ID 和审计内容哈希，状态与绑定审计事件原子写入；同一修订产生新回测只更新下一候选，不改变活动绑定。切换只允许在监控暂停、策略空仓且没有未决委托时完成，交接后仍保持暂停，不授权生产会话、不启动监控、不立即评估也不下单。运行中固定审计证据失效时禁止新增风险；已有持仓只按绑定时固定快照执行止损、止盈或退出。当前生产适配器仍只支持 `BTC/USDT · 1m`；组合风控与执行中心均只读或受控复用同一生产快照，不把研究组合或表单草稿解释为活动生产状态。
_Avoid_: 运行回测即实盘、接受研究候选即生产批准、AI 页面直接交接、保存策略即实盘、前端自行判定生产资格、持仓中换规则、浏览器传入策略正文、审计漂移后继续开仓、证据失效后停止退出、第二套策略引擎

**生产自动委托权限复核**：
受控自动生产现货会话开启后，每次确认原 `clientOrderId` 不存在、即将创建新委托前，再次读取 Binance 当前 API restrictions。IP 白名单、读取/现货权限或危险权限任一漂移都阻止创建；查询和对账既有委托不依赖该创建前复核。
_Avoid_: 只在会话开启时核验权限、权限漂移后继续创建、用创建权限阻断既有订单对账

**自动委托提交意图**：
测试网或生产自动交易调用外部路由前，以稳定 `clientOrderId` 幂等记录脱敏订单与 AI 决策，并在策略状态中分别保存已确认水位。若外部路由已接受委托但策略状态尚未保存时进程退出，重启只从同一 API 数据卷中 `runId` 为空且来源、阶段和委托结构完整的本地运行域事件恢复为 `submission_pending`，随后按原 ID 只读查询并收口终态；研究包禁止导入任何自动交易控制、策略绑定、委托、成交或尘埃事件。幂等事件 ID 已存在时，运行模式、订单、订单意图和交易意图必须与本轮规范内容完全相同，否则失败关闭且不得路由。
_Avoid_: 外部调用后才留痕、只恢复生产不恢复测试网、跨 K 线换 ID 下单、恢复时补单、研究导入恢复执行权、ID 冲突覆盖已校验委托

**自动委托终态对账**：
API 后端运行器对测试网或受控生产自动交易中已经提交但尚未终态的委托，持续使用原 `clientOrderId` 执行只读查询。暂停监控、会话过期或急停只阻止新委托，不能丢弃既有成交事实；终态成交只计入策略账本一次，未完成对账时不能切换执行模式。操作者可在暂停状态人工触发同一只读对账路径，该动作即使与后台运行器竞态也只查询既有委托，不能进入行情评估或创建下一笔订单。它不改变 Stage 6 权威批次的非轮询契约。
_Avoid_: 查询缺失后补单、新客户端订单 ID、急停后创建委托、人工对账复用评估入口、重复入账、切换模式清空未决订单

**自动成交幂等入账**：
测试网与生产自动成交使用原 `clientOrderId` 派生稳定交易 ID，并以审计事件唯一键写入。若成交审计已经提交、策略状态尚未保存时进程退出，重启对账会复用原成交审计及其时间，再把同一笔成交写入策略账本，不产生第二条成交记录。
_Avoid_: 随机实盘交易 ID、重启重复成交审计、用删除旧审计修复账本

**自动委托资金记账**：
自动市价买入按报价币成本提交，使交易所请求本身受单笔资金预算约束。策略账本优先使用交易所标准化成交金额与基础币/报价币手续费：报价币手续费扣减现金，基础币手续费扣减净持仓；缺少手续费证据或第三方手续费币无法估值时保留估算值并标记 `feeEstimated=true`。
_Avoid_: 参考价数量充当硬资金上限、固定费率伪装成实际手续费、基础币手续费仍计入可卖持仓、缺失费用不标记

**自动交易经济账本**：
动态交易页从既有自动交易快照只读展示当前执行模式账本的已实现盈亏、按最新策略价格计算的未实现盈亏、累计成交手续费和智能服务调用用量。已实现盈亏与持仓平均成本已经纳入成交手续费，累计手续费只作为成本归因展示，不能再次从交易盈亏中扣除；卖出时以基础币扣除的手续费按实际减少的持仓成本结算，不能按成交价重复扣费；现有成交可从同模式、同标的审计事件恢复费用，证据缺口必须明确标记。新版本累计已成功返回且元数据通过校验的外部模型调用遥测；调用异常、用量字段缺失或旧状态无法完整恢复时标记证据不完整。该遥测不是服务商账单，进程在响应后、状态保存前异常退出等缺口仍需服务商账单或稳定调用审计对账。没有服务商账单或显式历史价格时，智能服务金额成本与扣除该成本后的净盈亏保持 `null / unpriced`。Paper 与测试网金额明确为模拟；生产实盘另以 Binance Spot 权威账户快照展示整个专用现货账户的总净值，但策略已实现与未实现盈亏仍只按本系统成交归因，充值、提现、账户内兑换和其它资产重估不得伪造成策略盈亏。
_Avoid_: 虚拟任务收入、排行榜或时薪冒充交易营收、硬编码模型价格、把令牌数量直接当货币、手续费重复扣减、资金流伪造成策略盈亏、为经济展示新建第二套账本

**自动交易策略资金覆盖**：
测试网或受控生产模式每次进入行情与 AI 评估前，只读同步整个专用 Binance Spot 账户；交易所快照是实物资产数量、锁定数量和可用余额的权威来源，本地策略账本只保留成交归因与盈亏证据。所有能通过直接现货交易对按 USDT 计价的非零资产都纳入账户总净值；任一非零资产无法完整估值、余额结构不一致、账户读取失败、存在任何未决现货挂单或账户指纹与已绑定账户不同，都会失败关闭并停止新决策。首次采用权威快照建立风险基线；之后无法由本系统已确认成交解释的充值、提现或账户内兑换作为外部资金流同步资产结构，并按净 USDT 资金流等额调整初始、日初和峰值风险基线，不计入策略盈亏；等值兑换的净资金流为零。全局运行就绪不要求当前持有 BTC 或空闲 USDT；生成具体 BTC/USDT 委托时仍必须再次校验该订单实际需要的源币种可用余额，买入只使用可用 USDT，卖出只使用可用 BTC，不能自动兑换其它币种补足订单。
_Avoid_: 本地账本覆盖交易所实物余额、未完整估值仍运行、忽略人工挂单、账户指纹漂移后继续交易、外部资金流伪造成盈亏、用其它币种自动换汇补单

**自动交易尘埃处置**：
测试网或受控生产模式执行完整退出时，若剩余策略持仓仅因交易所最小数量或最小名义金额而无法卖出，系统不得放宽交易所规则或伪造成交，而应写入一次脱敏审计，把该残余从 BTC/USDT 策略持仓释放为账户内尘埃资产。该路径只适用于目标仓位为零的卖出和明确的最小交易规则拒绝，不提交委托，也不计入策略盈亏或成交记录；资产仍留在专用 Spot 账户时继续计入权威账户总净值，不得作为资本转出调整风险基线，也不能伪造成策略亏损。
_Avoid_: 放宽最小交易规则、把尘埃伪造成交、删除交易所真实资产、对部分减仓或一般拒单释放仓位

**自动交易可用行情**：
自动交易后端每轮为固定 `BTC/USDT · 1m` 请求“策略所需完整 K 线数量 + 当前形成中一根”；内置涨跌幅策略仍为 7 根请求、6 根完整 K 线，审计策略按共享条件求值器动态计算均线、相对强弱指标或成交量条件所需回看长度。剔除未收盘当前分钟后，完整数量不足或最新完整 K 线收盘超过两个周期时进入 `data_blocked`，不得继续检查账户、调用 AI 或提交委托。
_Avoid_: 当前形成中 K 线、过期缓存触发交易、前端时钟判断、用数据源 `is_complete` 代替时效检查

**M1 统一决策契约**：
把市场快照、决策提案和标准信号绑定为可哈希、可持久化、可回读的同一条证据链。自动交易先接入该契约，后续研究、回测与三种执行模式复用相同语义，不另建策略引擎。
_Avoid_: 松散最近判断、模式专属信号、第二套决策算法

**市场快照**：
统一决策契约使用的规范化完整 K 线集合，绑定市场、标的、周期、数据源、数据哈希和快照哈希。自动评估、研究运行及其内置回测按同一规则生成 `snapshotHash`，既有策略实验与 AI 评审的 `snapshotId` 承载同一身份；历史研究运行读取时从既有数据哈希补出该身份，回测工作区、AI 评审证据区和 Markdown 报告展示该身份，内容哈希继续单独用于数据完整性。相同上下文和内容必须生成相同哈希。
_Avoid_: 未收盘 K 线、仅绑定最新价格、前端临时快照

**决策提案**：
规则、风险或已记录 AI 响应针对一个市场快照给出的原始方向、置信度和理由。提案同时保存 Provider、模型、提示模板版本、输出协议版本、用量、延迟和快照证据引用；规则或风险提案对不适用的模型字段保留空值。AI 提案是审计输入，不是可重复调用的确定性函数，也不能绕过后续边界；通过内容哈希校验的已记录提案可以重新进入同一契约构建器，重放标准信号、组合目标、风险调整与订单意图，不再次调用 Provider。
_Avoid_: 丢弃 AI 调用身份与用量、AI 直接订单、覆盖原始提案、把提案当最终信号

**标准信号**：
决策提案经过当前确定性持仓边界后形成的买入、卖出或持有结论。信号记录稳定策略身份、策略修订、周期、到期时间和所评估的最后一根完整 K 线时间；自动交易与回测共享完整提案、信号构造器和同一条持仓边界规则：已有持仓不重复买入，空仓不可卖出。回测信号只绑定截至当时已评估 K 线的不可变前缀快照，不引用未来数据；研究运行另以整段快照记录完整回测输入。回测成交记录及研究审计保存提案 ID、信号 ID 和信号快照哈希；回测结束时的强制平仓只属于模拟结算，不绑定虚构信号。提案与信号可以不同，但必须通过提案 ID、快照哈希和策略修订保持可追溯。
_Avoid_: 丢失原始提案、无法关联快照、信号直接提交订单

**组合目标**：
标准信号在当前持仓、参考价、当前交易对可用报价币和单笔预算下生成的目标持仓，明确记录当前数量、目标数量、变动数量和目标名义金额。后续执行只能消费目标差额，不能重新推导另一套数量。
_Avoid_: 信号直接下单、路由层重新计算目标、目标脱离策略账本

**风险调整目标**：
风险阶段对组合目标作出的确定性结果，只允许保持、缩减、清零或拒绝，并绑定亏损回撤、亏损回撤上限、盈利峰值回撤、盈利回撤上限、小时成交计数和频率上限证据。Paper 使用本地管理权益；测试网与生产使用权威 Spot 账户总净值。亏损回撤只比较风险日初管理权益与当前管理权益；盈利回撤只在权益高于日初并形成日内峰值后，比较峰值权益与当前权益。两者独立配置、独立计算，任一达到上限即锁存到下一 UTC 风险日并拒绝新的买入或加仓，但不能阻止止损、止盈、对账或其它降低风险的退出。已识别的外部充值、提现和账户内兑换同步调整日初与峰值基线，不得伪造成任一种回撤。
_Avoid_: 风险放大目标、频率上限阻止清仓、无证据调整、隐式改写组合目标

**订单意图**：
风险调整目标存在非零批准差额时生成的确定性市价委托描述，绑定市场快照、策略修订、提案、信号、组合目标、风险调整目标和已通过的账户覆盖检查，并记录标的、方向、规范数量、参考价、名义金额、交易场所精度、最小数量、最小名义金额、费率估算和滑点模型。Paper 使用本地策略账本检查、既有估算费率与零滑点；测试网和生产复用现有 Binance Spot 准备步骤，在交易对、CCXT 精度、市场限制、该笔委托所需源币种可用余额、急停、会话、未决状态及生产权限通过后才生成最终意图，提交前再次执行相同交易场所检查。其它现货资产只贡献账户总净值，不会被自动兑换为该笔订单的源币种。相同证据必须生成相同订单意图 ID。无批准差额或任一前置检查失败时不生成订单意图；测试网或生产路由的稳定 `clientOrderId` 从执行模式和最终订单意图 ID 派生，并直接消费意图中的规范数量。
_Avoid_: 持有信号空委托、未通过账户检查仍生成委托、自动兑换其它资产补单、路由重新计算数量、订单脱离决策证据、时间戳充当订单身份

**订单结果**：
纸面、测试网或生产实盘对一个订单意图产生的统一执行事实，必须绑定原订单意图 ID，并记录执行模式、订单状态、成交数量、剩余数量、成交均价、成交金额、费用明细及费用是否仍为估算。测试网和生产结果由既有路由回执及只读对账推进并保留交易所费用币种；Paper 使用同一字段语义记录本地报价币费率估算，但不伪造外部订单号。
_Avoid_: 模式专属结果结构、结果脱离订单意图、纸面伪造交易所回执、另建订单状态机

**自动交易运行心跳**：
自动交易后端运行器每轮持久化累计轮次、最近心跳、最近成功、最近异常和连续失败次数；异常后的下一次成功会清零连续失败并把 `evaluation_error` 恢复为监控状态。执行中心按三个运行周期且至少 90 秒判断心跳中断。心跳只用于运维可见性，不自动重启、不调用 AI，也不改变风险或委托边界。
_Avoid_: 前端维持后台任务、心跳自动下单、心跳自动重启、用心跳覆盖账户或订单异常、记录敏感行情或余额

**自动交易运维提醒**：
执行中心使用浏览器原生系统通知提示未决委托、账户或风控异常、状态 API 读取失败，以及后台停止、连续失败或心跳中断。只有用户主动点击并授予权限后才发送；同一异常状态只提醒一次，成功恢复读取或恢复正常后才允许再次提醒，且页面必须保持打开。提醒只投影已有状态，不执行评估、对账、重启或委托。
_Avoid_: 自动申请通知权限、每次前端刷新重复提醒、把参数校验失败误报为后台断线、第三方推送服务、把通知当作后台常驻服务、通知触发交易操作、发送余额或订单号

**M2 服务端监控任务**：
API 进程中的独立只读观察者，按固定任务 ID 持久化最近轮次、成功、错误、连续失败和下次可运行时间，并把自动交易运行器投影为独立受观察任务。它不调用行情、AI、对账、重启或委托入口；浏览器关闭后仍随 API 运行。Crypto 使用连续调度，A 股任务复用现有市场日历计算下一可运行时刻，不因此自动创建研究运行。
_Avoid_: 第二套自动交易运行器、通知线程调用业务命令、页面心跳充当服务端任务、无任务需求的通用调度框架

**Docker 服务自动恢复**：
主 Compose 中的 API 与 Web 使用 `unless-stopped` 重启策略，使进程异常退出或 Docker 守护进程恢复后重新启动服务；操作者显式停止时保持停止。该能力只恢复原服务和持久数据卷，不执行评估、对账、模式切换、权限恢复或委托动作。
_Avoid_: 健康检查冒充自动重启、显式停止后强制拉起、恢复容器时自动开启生产权限、额外守护进程

**M2 服务端监控事件**：
由已有自动交易状态派生的持久事件，使用不含动态错误文本的稳定 `incidentKey`，记录 `active / resolved` 生命周期、发生次数、首次出现、最近观察、恢复时间、严重度和下一步。相同发生周期只投递一次开启提醒和一次恢复提醒；恢复后再次出现会增加发生次数并生成新的去重键。
_Avoid_: 每轮新建告警、错误文本充当身份、通知结果改写交易状态、恢复后沿用旧发生周期

**M2 外部通知投影**：
服务端监控事件向一个可选 Webhook 的脱敏只写投影。投递意图先以稳定去重键写入现有审计账本，再执行一次网络请求；URL 首次从环境变量初始化，用户保存平台设置后改由加密数据库配置提供，并立即更新运行中的监控服务，不要求重启 API。设置页只在已保存 Webhook 后允许发送一条明确标记为测试、且不代表真实交易事件的脱敏消息；测试结果进入现有通知审计，URL 原值不进入 API、审计或页面。投递失败只记录失败证据，不影响交易、对账、审计事件或后续监控轮次。浏览器原生提醒继续保留为页面打开时的本地渠道。
_Avoid_: Webhook 命令回调、投递失败暂停交易、明文保存或返回 Webhook URL、测试消息伪装真实事故、发送余额或订单号、多个渠道框架

**M3 数据源能力矩阵**：
在既有设置状态中明确列出每个行情适配器支持的市场、周期、单次历史深度、复权模式、时效语义、凭据要求和只读边界。配置存在不代表健康；未实际探测的可选来源保持待观察，且端点值不进入 API 或页面。
_Avoid_: 配置等于可用、返回端点或凭据、100 个连接器目录、第二套适配器注册表

**M3 数据快照质量契约**：
既有研究快照与 `DataQuality` 共同记录来源、观测时间、市场时间、日历身份、复权、覆盖率、时效、警告和规范哈希。统一检查复用现有 K 线规范化，检测重复、乱序、非法 OHLC、非有限值、缺口和形成状态；结构错误阻断研究，股票日线静态日历局限和形成中 K 线显式告警，自动交易继续使用原有更严格完整 K 线与时效闸门。
_Avoid_: 第二套 K 线 schema、静默清洗、节假日模板冒充交易所日历、放宽自动交易数据闸门

**M3 跨来源差异报告**：
首版只比较 A 股日线 OHLCV 的共同时间戳，按字段记录一致、警告、阻断阈值、重叠率、差异明细和报告哈希，并固定 `valuesMerged=false`。材料差异在回测和 AI 前阻断；财务事实双来源验证留在 M4 的 AI 研究证据范围。
_Avoid_: 平均来源、静默选值、通用数据融合框架、把对照源提升为唯一权威

**M3 离线快照回放**：
新研究运行把规范 K 线嵌入现有不可变快照，写入与回读时重新验证同一内容哈希并声明无需网络；上游失败时可使用既有 SQLite 缓存形成同哈希快照。历史运行不迁移、不补写数据库。
_Avoid_: 回放重新拉公网、缓存值冒充新鲜行情、数据库迁移、只校验行数不校验内容

**M3 free-stockdb 只读对照源**：
可选本地 A 股日线适配器，只调用官方 `cmd=get` HTTP 读取协议并用于跨源比较，不调用同步、计算或写入命令，不替换主来源。未配置、请求失败或质量不合格都形成不可用报告。
_Avoid_: 强制依赖、自动启动或同步上游、写入本地服务、唯一权威来源、泄漏服务地址

**M4 AI 研究证据**：
从权威 `aiqt.aiReviewRun` 派生并写入既有审计账本的只追加研究记录。事实、计算、假设和模型推断分别标记并保留证据引用；信息丰富度只评价材料覆盖，投资确定性单独评价当前判断，两者不互相推导。研究建议绑定原评审、研究运行、市场快照、参考时间、参考价和声明周期，但没有仓位、风险授权或订单语义。
权威 AI 评审在用户逐次授权外发后，使用服务端冻结快照中的原始已完成 K 线及其快照/内容哈希，不发送形成中或未来 K 线。外部响应若在本地执行语义安全校验中被拒绝，只审计响应已返回、校验阶段、错误类别和字段路径，不保存或回显被拒绝原文；本地确定性评估继续有效。
_Avoid_: 混合证据类型、丰富度冒充确定性、AI 结论直接进入执行链、第二套 AI 评审 Store

**M4 A 股财务事实双源报告**：
针对营收、净利润、经营现金流、总资产、股东权益和每股收益，保留两个来源标识不同的原始观测，校验报告期、单位和相对差异，分类为 `agreement`、`warning` 或 `blocked`，并固定 `valuesMerged=false`。未提供观测时明确为 `unavailable`；首版接收冻结或人工确认的结构化来源，不静默抓取、平均或选择公共来源。
_Avoid_: 同源伪装双源、单位或报告期错配、静默选值、平均来源、通用财务仓库

**M4 可选三视角研究**：
对同一份已审计证据生成 bullish、bearish 和 neutral 三个研究投影，只允许日线或周线显式启用。它不增加 Provider 调用、不引入 Agent 编排，也不能进入分钟级自动交易热路径。
_Avoid_: LangGraph、多 Agent 状态机、分钟级调用、三视角投票下单、重复外部 AI 请求

**M4 到期结果复盘**：
只有声明周期后的同市场、同标的、同周期审计运行和一份不同标的的同市场同周期基准运行齐备时，才记录原始收益、观点调整收益、最大不利波动、基准收益和 alpha。结果绑定原建议、AI 评审、源/结果/基准运行及快照；历史教训只进入后续研究上下文，固定不影响风险、授权、权限和订单路由。
_Avoid_: 周期未到提前判定、无基准归因、跨上下文拼接、结果学习自动改风控或下单

**M5 账户组合匹配**：
从已审计 Stage 4 纸面账户回放读取现金、权益、持仓和最新价格，再与同一工作流的本地目标腿逐项匹配。账户中存在本地组合未覆盖的持仓时，整个候选调仓批次阻断；系统不隐藏、不代卖也不自动吸收到目标组合。
_Avoid_: 前端估算当前权重、忽略未知持仓、账户镜像表、用本地目标覆盖真实回放

**M5 风险调整组合目标**：
在原组合目标上应用总暴露、单一标的、行业、市场和币种集中度限额，只允许保持或缩减每个标的目标，缩减部分回到现金。当前、原目标、调整目标、漂移和候选调仓必须同时保存并满足权重守恒。
_Avoid_: 风险增加目标、丢失原目标、缩减后不补现金、路由层重新计算组合

**M5 组合批次风险评估**：
把最大回撤、当日损失、交易频率、总暴露、集中度、相关性和协方差风险贡献作为同一候选批次的权威检查。阻断项会阻断整批；可缩减项只降低目标。结果写入既有 `AuditEventStore`，只生成 paper-only 候选，不审批、不提交、不路由订单。
_Avoid_: 单笔检查冒充组合风控、越限后部分偷跑、第二套协方差算法、新建组合风险数据库

**组合风控生产运行投影**：
组合风控保留阶段 4 / M5 研究模拟链，并从既有自动交易状态只读投影当前策略绑定、运行器健康、账户覆盖、双回撤、小时成交额度、风险调整目标和生产授权。生产健康必须同时满足后台心跳、风险停机、完整账户估值、无未决挂单、账户指纹一致、绑定证据以及既有生产门禁；最近一次决策不能替代当前策略绑定。页面分别标示 Binance Spot 权威账户总净值与本系统成交归因的策略盈亏，不能把二者混成一个指标。当前生产后端只支持单策略、单标的；仅当研究组合恰好为一个组合腿且审计运行与生产绑定一致时声明覆盖，否则明确显示为独立生产链。页面只允许刷新状态和导航到动态交易复核。
_Avoid_: 单策略实盘冒充多标的组合已接入、最近决策冒充当前绑定、账户净值冒充策略收益、前端新增绑定/授权/启停/评估/订单入口、第二套生产组合状态机

**M6 连接器状态投影**：
数据源复用设置状态、Provider 健康和缓存证据；AI Provider 复用既有注册表；执行适配器复用设置状态、台账、只读健康探测和证据链。总览先显示状态、阻断原因、影响和下一步；配置只代表存在，不能推导为健康或已授权。
_Avoid_: 第二套连接器注册表、配置等于健康、静态 Provider 冒充运行状态、前端推断下单权限

**M6 终端渐进披露**：
所有新版工作区通过共享页头显示当前状态、阻断原因和下一步。连接器能力、权限、冷却、最近成功或状态证据、未决状态和技术诊断默认折叠，展开后的宽表只在自身容器滚动。
_Avoid_: 每个工作区重复状态逻辑、第一屏摊开原始证据、100 个连接器目录、大而全终端

**M6 数据源健康证据校正**：
Provider 错误必须与真实数据 `source` 匹配，完整数据的普通质量警告不计入 Provider 故障。free-stockdb 配置完成后由设置页追加一次受超时限制的 `600000 / 1d / limit=1` 只读探测；只有探测返回完整数据才显示健康，失败仍待观察，且不调用同步、计算或写入命令。
_Avoid_: 按市场猜测 Provider、质量警告制造冷却、配置直接等于健康、后台同步本地数据

**平台持久化设置**：
首次配置前由 API 进程环境变量初始化；用户从设置页首次保存后，一条 SQLite 配置记录成为受支持字段的权威来源。普通值可回读编辑，密钥与私密 URL 使用 AES-256-GCM 加密保存，API 只返回是否已配置和固定掩码；留空保持原密钥，清除必须显式选择。每次保存后，同一有效配置会立即刷新 Finnhub 报价、CCXT K 线、AI Provider、Free StockDB、监控 Webhook、测试网路由、生产只读/交易路由和后台自动交易服务，不要求重启 API；已有未决委托、策略账本和急停保持原状态。自动评估间隔可在设置页配置为 `5–3600` 秒，默认 `35` 秒；运行中的后台运行器最迟约一秒采用新间隔，保存本身不触发评估。生产授权时长可在设置页或执行中心修改，默认八小时，`0` 表示下一次实名授权永久有效。生产实盘总开关 `productionTradingEnabled` 默认关闭，首次保存前可由 `AIQT_ENABLE_PRODUCTION_TRADING` 初始化，保存后由数据库配置接管并即时刷新运行中的生产路由；开启只解锁生产路由，不会切换自动交易模式、开启监控、授权或续期会话、评估行情或提交委托，关闭则立即阻止新的生产委托而不妨碍既有未决委托的只读对账。生产凭据、最新权限与 IP 核验、急停控制、实名确认、余额、交易所规则和风控门禁继续独立生效。
_Avoid_: 响应返回密钥原文、空输入意外清除密钥、数据库配置与环境变量混用、保存凭据自动启用实盘、总开关绕过权限或实名门禁、保存时长静默扩大当前授权、要求重启 API 才应用网页配置

**生产执行状态投影**：
设置页、执行中心与全局底栏从自动交易权威快照读取当前 `paper / testnet / live` 模式、生产凭据、控制证据新鲜度、会话确认、授权时长和授权期限。已配置适配器不等于生产授权；过期启动证据会明确显示，但不会追溯关闭已经绑定同一活动控制且仍有效的生产会话，因为每笔新委托前仍会实时复核 Binance 权限。`0` 小时授权显示为永久有效；证据过期时仍不能开启或重新绑定生产会话。
_Avoid_: 固定实盘阻断文案、内部阶段编号冒充用户状态、控制记录 active 冒充证据仍新鲜、保存密钥自动开启生产会话

**执行中心当前控制面**：
普通执行中心只显示当前自动交易运行台账、风险参数、生产交易控制链，以及仅在测试网模式下出现的测试网急停。阶段 5–9 的手工影子、批次、生产只读和旧单笔准入工作台不属于当前自动交易路径，不再提供普通页面操作入口；相关事件统一在审计回放的“历史执行验收证据”中按阶段只读展示，不能从审计页授权、急停、恢复或提交委托。
执行中心的页头、自动交易区、生产权限区和全局底栏共享同一自动交易快照。页头只按已持久化模式和顶层生产门禁显示纸面、测试网、生产阻断或有效会话；未保存的表单模式不能提前改变当前状态。快照读取失败时立即清空前端可信投影，不得回退设置缓存继续宣称生产会话或生产路由可用；实名授权或续期后立即回读权威快照。策略概览必须读取 `strategyBinding`，已审计策略隐藏只适用于内置策略的智能决策服务和涨跌幅阈值；绑定阻断时不能开启监控或触发新评估，既有未决委托的只读对账不受影响。有效生产会话与续期证据分开显示：启动核验证据过期不追溯关闭当前有效会话，但会阻止续期或重新授权，且每笔新委托继续复核全部门禁。
_Avoid_: 旧验收工作台冒充当前配置、Stage 9 候选驱动自动交易、生产模式显示测试网急停、审计证据携带写操作、表单草稿冒充运行状态、状态读取失败后继续显示旧快照、已审计策略继续显示内置触发参数、续期证据过期冒充当前会话已撤销、第二个自动交易轮询

**运行管理生产运行投影**：
运行管理从既有自动交易快照和服务端监控接口只读汇总当前模式、生产策略绑定、后台心跳、最近判断、委托结果、策略账本风险、账户覆盖、生产授权和服务端事件。自动交易状态不可用时保持读取失败，不回退为纸面模拟；策略绑定证据缺失时保持待确认，不以最近判断替代。原研究队列、扫描器、研究验证节点、数据源健康与市场日历继续作为次级研究维护工具，并明确其模拟证据不代表生产运行；AI 已评审候选只导航执行中心复核生产交接，不再自动提交模拟委托。页面只提供动态交易、执行授权和审计回放导航，不执行评估、对账、授权、急停、模式切换或委托。
_Avoid_: 运行管理第二套状态、读取失败回退模拟态、研究模拟证据冒充生产运行、运维页面自动触发交易动作

**自动化交易流程引导**：
所有工作区共享的普通用户路线，固定按设置、行情、研究、策略、回测、AI 评审、组合风控、执行、动态交易、运行和审计串联十一个现有页面。裸地址默认进入行情中心；流程条当前项始终跟随用户正在查看的工作区，黄金路径下一目标驱动唯一主按钮。点击后由一次性的页面内控制器自动切换到每个需要操作的工作区，并串行复用现有刷新、研究流水线、AI 评审和本地模拟执行动作；切换标的或周期时沿用当前可编辑策略，只清除不再匹配的运行与审计结果，刷新生成的新 K 线和刷新证据就绪后才运行研究流水线，并把同一次刷新运行 ID 绑定进研究快照。刷新和流水线动作只有在当前上下文确实生成成功证据时才算执行成功；失败时原地停止并显示该动作已经取得的具体错误。每步完成后重新读取服务端黄金路径，状态未推进即停止，不保存第二份流程进度。动态交易页只投影既有自动交易快照、行情图、决策契约、风险边界、委托结果和运行控制，不保存第二份交易状态，也不新增交易算法或路由。完整且新鲜的数据质量警告保留审计提示但不形成重复刷新，不完整、失败或未知来源仍阻断。流程到达执行交接、实盘适配器认证、实名确认、生产权限、急停或风控闸门时必须暂停并交还人工处理；其中实盘闸门入口在当前页面直接弹出并复用既有“生产交易控制链”，打开前只读刷新自动交易权威快照。生产总开关已保存且控制恢复后，实名操作人在同一窗口显式确认真实资金风险：当前已启用 `live` 模式时只续期生产会话，仍处于 `paper / testnet` 或暂停的 `live` 模式时通过既有自动交易配置端点显式切换并启动生产会话；启动请求本身不立即评估，但后台监控启用后下一周期可能在全部门禁通过时提交真实委托。成功后刷新黄金路径并关闭弹窗；旧 Stage 9 单笔候选复核不属于自动生产会话门禁。设置页继续只负责连接器与安全状态总览，并必须明确区分未保存草稿与已保存总开关。入口打开本身不执行权限核验、不恢复生产控制、不开启生产会话，也不调用生产订单路由。
_Avoid_: 第二套流程状态机、一键绕过人工闸门、把页面访问当作业务完成、隐藏阻断原因、流程完成即自动开启实盘

**动态交易运行状态投影**：
黄金路径继续只计算六步研究与实盘准入闭环，同时把既有自动交易快照中的运行器健康作为动态交易工作区状态；后台连续失败、心跳中断或评估阻断会显示真实原因，健康运行则显示就绪。运行状态卡只有在自动监控启用时才按本地一秒时钟显示下一次评估倒计时；暂停后立即显示暂停和空倒计时，五秒状态轮询只负责同步后端事实。六步完成后根据真实 `liveTradingAllowed` 区分模拟闭环与生产自动交易可用，不再固定显示“实盘仍需认证”。共享现货订单准备在调用 CCXT 精度归一化前先检查交易所最小数量，使无法交易的退出尾仓进入既有审计释放路径，而不是形成重复后台异常。
_Avoid_: 前端猜测运行器健康、完成态固定写死认证状态、复制自动交易状态机、把低于最小数量的尾仓无限重试

**A 股全市场发现与条件选股**：
行情中心通过只读全市场快照展示股票数量、上涨、下跌、平盘和成交额，并按代码或名称、涨跌幅、成交额、换手率、市盈率及白名单排序生成研究候选。快照优先分页读取东方财富公开行情，失败时降级到已安装的 AKShare 新浪实时行情，再失败时只允许使用最近一次成功快照并标记过期；首次无可用来源则明确不可用。成功快照缓存五分钟并生成规范哈希，候选只复用现有自选、行情与研究入口，不落库、不自动运行研究、不进入订单或自动交易链。
_Avoid_: 自选列表冒充全市场、单页前 100 条冒充全集、free-stockdb 对照源冒充选股权威、上游失败时伪造行情、筛选结果直接下单、第二套研究或交易状态机

**Binance USDT 现货市场发现与条件筛选**：
行情中心在当前标的为加密货币时复用同一只读市场发现契约，联合 Binance 公开 `exchangeInfo` 与滚动 24 小时行情，只纳入状态为可交易、允许现货交易且计价资产为 USDT 的交易对。成交额使用 `quoteVolume`（USDT），成交量使用基础资产 `volume`，不混合其它计价资产；快照独立缓存五分钟，刷新失败时仅使用最近成功快照并明确标记延迟，三十秒重试冷却避免并发请求重复阻塞上游。页面隐藏 A 股专属的换手率、估值和市值字段，跨市场切换会清空不兼容的筛选条件；筛选结果只进入既有自选与研究入口，当前生产自动交易标的仍固定为 `BTC/USDT · 1m`。
_Avoid_: 把 Binance USDT 现货称为全加密市场、把滚动 24 小时口径称为自然日、跨计价资产汇总成交额、把基础币成交量相加当作市场总量、从市场筛选直接触发委托

**可审计 AI 选股**：
行情中心在既有条件筛选后，由核心服务重新生成权威候选并固定执行 `100 → 20 → 5`：A 股覆盖全市场、加密资产覆盖 Binance USDT 现货、美股只覆盖现有自选池。候选必须绑定当前市场快照、180 根已完成日 K、技术因子和适用的基本面事实；A 股保留财务报告期与双源冲突，美股使用配置了合规 User-Agent 的 SEC Company Facts，加密资产使用 CoinGecko 的 Binance 交易对 `coin_id` 精确映射。确定性风格评分先完成预筛与排名，外部 AI 只能在合格候选中重排并用中文解释；失败时明确降级为本地基准榜。完整证据和模型输出使用 `market_ai_selection / aiqt.marketAiSelection` 写入既有审计链。结果只分为优先研究、观察和证据不足；逐只“开始研究”只携带选股与候选两个身份切换到既有日 K 研究上下文，不自动加入自选或运行研究。用户显式运行研究时，后端重新校验受保护选股事件、记录哈希、候选、市场和标的，并把服务端派生的来源证据及独立哈希写入现有研究快照；手动换标的或周期会清除待绑定来源。该链路仍不创建第二套策略、组合、仓位、授权或订单状态。

选股到期复盘只计算已经达到固定持有根数且行情完整的推荐，未到期保持“观察中”，缺失同周期审计基准时明确记为数据不足。收益、基准价格与最小 K 线事实均由服务端生成并写入受保护审计，质量统计再回放选股、研究绑定、基准运行、规范哈希和完整推荐集合；页面始终显示推荐、到期、未到期、数据不足、绝对命中、相对基准命中及样本量，并展示候选合格率、主要排除原因、选股运行数据源降级率、AI 成功率和各风格历史表现。没有足够到期样本前，不启用批量研究或自动加入观察池；AI 选股始终不得直接连接订单或生产交易。
_Avoid_: 浏览器上传候选行情、缺失基本面填零、同名币种猜测、AI 虚构事实或置信度、推荐文本包含买卖/仓位/目标价/收益保证、批量研究、选股直接进入自动交易

**独立市场资讯工作区**：
市场资讯是行情中心之外的只读研究入口，使用独立的 A 股、美股和加密货币按钮选择资讯市场，不再随全局自选标的跨市场切换；当前自选恰好属于所选市场时才附带标的资讯。页面先单独读取新闻，再继续加载较慢的全市场快照、涨幅领先和成交活跃；新闻使用核心服务的 `scope + limit + offset + hasMore` 契约按 20 条逐页读取，筛选在服务端分页前完成，翻页只刷新新闻而不重新运行排行，切换市场、标的或筛选时回到第一页。A 股市场快讯按东方财富 `sortEnd` 批次读取，标的资讯按搜索页读取；为限制公开来源扇出，单次浏览的 `offset` 暂限制为 `0–1000`。美股与加密货币新闻复用已配置的 Finnhub 当前可用新闻窗口，密钥只通过请求头发送并随平台配置实时更新。资讯只保留标题、有限摘要、来源、发布时间、关联标的和原文链接，使用五分钟内存缓存；上游失败时仅回退最近成功分页快照并明确标记延迟或来源降级。该页面不进入自动交易流程步骤，不保存全文或图片，不自动运行研究、AI 评审、风控或订单。
_Avoid_: 本地审计事件冒充新闻、复制文章全文、把公开网页端点称为官方稳定 API、资讯情绪直接驱动交易、第二套行情快照或交易状态机

**Shadow Execution**：
把权威组合意图投影到隔离的本地适配器，不连接交易所也不提交委托。
_Avoid_: Sandbox 委托路由、Paper Trading
