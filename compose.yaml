services:
  api:
    build:
      context: .
      dockerfile: Dockerfile.api
      args:
        INSTALL_DATA_DEPS: ${INSTALL_DATA_DEPS:-false}
    restart: unless-stopped
    environment:
      QUANT_CORE_HOST: 0.0.0.0
      QUANT_CORE_PORT: "8765"
      AIQT_SETTINGS_MASTER_KEY: ${AIQT_SETTINGS_MASTER_KEY:-}
      FINNHUB_API_KEY: ${FINNHUB_API_KEY:-}
      CCXT_DEFAULT_EXCHANGE: ${CCXT_DEFAULT_EXCHANGE:-binance}
      CCXT_TIMEOUT: ${CCXT_TIMEOUT:-10000}
      AIQT_LIVE_SESSION_TTL_HOURS: ${AIQT_LIVE_SESSION_TTL_HOURS:-8}
      CCXT_SANDBOX_API_KEY: ${CCXT_SANDBOX_API_KEY:-}
      CCXT_SANDBOX_SECRET: ${CCXT_SANDBOX_SECRET:-}
      CCXT_PRODUCTION_READONLY_API_KEY: ${CCXT_PRODUCTION_READONLY_API_KEY:-}
      CCXT_PRODUCTION_READONLY_SECRET: ${CCXT_PRODUCTION_READONLY_SECRET:-}
      CCXT_PRODUCTION_TRADING_API_KEY: ${CCXT_PRODUCTION_TRADING_API_KEY:-}
      CCXT_PRODUCTION_TRADING_SECRET: ${CCXT_PRODUCTION_TRADING_SECRET:-}
      AIQT_ENABLE_PRODUCTION_TRADING: ${AIQT_ENABLE_PRODUCTION_TRADING:-false}
      AIQT_MONITORING_WEBHOOK_URL: ${AIQT_MONITORING_WEBHOOK_URL:-}
      AIQT_MONITORING_WEBHOOK_TIMEOUT_SECONDS: ${AIQT_MONITORING_WEBHOOK_TIMEOUT_SECONDS:-5}
      AIQT_FREE_STOCKDB_URL: ${AIQT_FREE_STOCKDB_URL:-}
      AIQT_FREE_STOCKDB_TIMEOUT_SECONDS: ${AIQT_FREE_STOCKDB_TIMEOUT_SECONDS:-3}
      HTTPS_PROXY: ${HTTPS_PROXY:-}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      OPENAI_MODEL: ${OPENAI_MODEL:-}
      OPENAI_COMPATIBLE_BASE_URL: ${OPENAI_COMPATIBLE_BASE_URL:-}
      OPENAI_COMPATIBLE_API_KEY: ${OPENAI_COMPATIBLE_API_KEY:-}
      OPENAI_COMPATIBLE_MODEL: ${OPENAI_COMPATIBLE_MODEL:-}
      OLLAMA_BASE_URL: ${OLLAMA_BASE_URL:-http://host.docker.internal:11434}
      OLLAMA_MODEL: ${OLLAMA_MODEL:-}
    volumes:
      - quant-data:/app/data
    expose:
      - "8765"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8765/health', timeout=3).read()"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
      args:
        VITE_QUANT_API_BASE: /
    restart: unless-stopped
    depends_on:
      api:
        condition: service_healthy
    ports:
      - "${AIQT_WEB_PORT:-5173}:80"
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "/dev/null", "http://127.0.0.1/"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

volumes:
  quant-data:
